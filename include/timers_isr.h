#ifndef TIMERS_ISR_H_
#define TIMERS_ISR_H_

#include "microwave_firmware.h"

// ============================================================================
// --- 🟨 ПРОТОТИПИ ФУНКЦІЙ ---
// ============================================================================

/**
 * @brief (v2.9.32) Налаштовує TIMER1 для генерації 500мкс (2000Hz) переривання.
 * Використовується для 1000Hz тону та 1ms системного тіка.
 */
void setup_timer1_500us(void);

/**
 * @brief Запускає "важкі" 1-секундні завдання з головного циклу loop().
 * (Викликається, коли ISR(TIMER1_COMPA_vect) встановлює g_1sec_tick_flag).
 */
void run_1sec_tasks(void);

/**
 * @brief Оновлює глобальний годинник (g_clock_hour/min/sec).
 * Викликається або з ZVS ISR, або з run_1sec_tasks().
 */
void update_clock(void);

/**
 * @brief Запускає короткий (300мс) звуковий сигнал.
 */
void do_short_beep(void);

/**
 * @brief Запускає довгий (800мс) звуковий сигнал.
 */
void do_long_beep(void);

/**
 * @brief Запускає серію сигналів для "перевертання".
 */
void do_flip_beep(void);

// (v2.9.0) ВИДАЛЕНО ФУНКЦІЇ ДЛЯ ASYNC TIMER2 RTC

#endif // TIMERS_ISR_H_